# Apex Legal AI - Environment Variables

# Gemini API Key (Required for the Analysis Agent to classify comments)
GEMINI_API_KEY=your_gemini_api_key_here

# Optional: Host/Port configuration for FastAPI (if running backend separately)
# API_HOST=0.0.0.0
# API_PORT=8000
